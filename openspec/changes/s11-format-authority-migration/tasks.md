> **Scope note (2026-07-10, operator directive):** `canon migrate` (the
> corpus rewrite tool), the in-place migration of the donor consumer repo's real
> corpus, and the `parity.py` sync patch are REMOVED from this change's
> scope. The donor consumer repo will conform its own corpus to canon's format on its
> own schedule and with its own review discipline; a one-shot migration
> script, if one is ever needed, is a separate, later, throwaway concern
> per consumer repo — not something this change ships or maintains.
> Task groups 3, 5, 6, and 7 below (and the write-action halves of a few
> task group 4 items) are marked **NON-GOAL** for this reason and are
> intentionally left unchecked. The schema/layout/`canon fmt --check`/
> fixture work those groups partially depended on is unaffected and is
> checked off where the committed code backs it.

## 1. Schema registry + layout descriptor

- [x] 1.1 Add versioned JSON-schemas to canon-model for: ledger records
      (run/review/clear/drill/code-review/design-review), divergence
      manifest/review/remediation events, inventory entries, policy,
      trajectories, strategies, sessions.
      Evidence: `crates/canon-model/src/family/{ledger,divergence,
      inventory,feature,policy}.rs` (commit 786fbf45) + eleven
      `schemas/family-*.schema.json` files generated by
      `schema_export::family_schemas()`/`cargo xtask write`.
      trajectories/strategies/sessions already had schemas from S1
      (`schemas/{trajectory,strategy_item,session}.schema.json`,
      RecordKind's closed twelve) — task 1.1 confirms they count
      toward "the family", not that they needed re-authoring.
- [x] 1.2 Implement `LayoutDescriptor {kind, partition_keys, leaf_grammar}`
      and register one per kind, including the zero-partition-key exception
      for `run`/`drill`.
      Evidence: `crates/canon-model/src/family/mod.rs`'s
      `LayoutDescriptor`/`LeafGrammar`/`FamilyKind::layout_descriptor()`
      (commit 786fbf45); test
      `run_drill_zero_partition_keys_is_not_flagged_missing_area`.
- [x] 1.3 Implement `layout_problem(record, path, descriptor)`, generalizing
      `parity.py`'s `_ledger_layout_problem` to every registered kind.
      Evidence: `crates/canon-model/src/family/mod.rs::layout_problem`
      (commit 786fbf45), exercised by all 11 `FamilyKind` variants via
      `canon-fmt`'s `resolve.rs`+`check.rs`; four unit tests plus the
      real-corpus run (2.3 below) proving it fires correctly on 6185
      real files.

## 2. `canon fmt --check`

- [x] 2.1 Wire `canon fmt --check <corpus-root>` validating every file
      against its kind's schema + layout descriptor.
      Evidence: `crates/canon-fmt/src/check.rs::check` (commit 4e69431d);
      `crates/canon-cli/src/main.rs`'s `Fmt` subcommand + `run_fmt`,
      `crates/canon-cli/src/fmt.rs` (commit 4e8b758a).
- [x] 2.2 Emit a violation report matching the audited gap categories
      (partition grammar, missing envelope, missing actor/session identity,
      free-text/joined refs, abbreviated shas, one-way back-refs).
      Evidence: `crates/canon-fmt/src/report.rs::FmtFailureClass` — the ten
      audited categories, one variant each (commit 4e69431d); exercised end
      to end by `crates/canon-fmt/tests/fixtures_check.rs::
      fmt_check_detects_every_audited_gap_category_on_fixtures`.
- [x] 2.3 Run `canon fmt --check` over `canon-fmt`'s LOCAL fixture corpus
      (reproducing the donor consumer repo's real, audited drift shapes — never the
      live the donor consumer repo checkout, per the 2026-07-10 operator directive) and
      confirm the report matches the 2026-07-10 audit table exactly.
      Evidence: `crates/canon-fmt/fixtures/consumer-corpus/pre/spec/**`
      (task 8.1) + `crates/canon-fmt/tests/fixtures_check.rs`'s three
      tests (`fmt_check_detects_every_audited_gap_category_on_fixtures`,
      `fmt_check_flags_the_pre_migration_features_dir_as_layout_violation`,
      `fmt_check_flags_assets_lock_as_a_fourth_ad_hoc_format`), plus
      `crates/canon-cli/tests/fmt_check.rs::
      fmt_check_exits_nonzero_and_lists_audited_gap_categories` (via the
      built `canon` binary) — all green.

## 3. `canon migrate` — features/ and inventory/ partition lift — NON-GOAL

- [ ] 3.1 NON-GOAL — `canon migrate`'s `features/` rewrite action (see
      scope note). The `features/` Hive partition GRAMMAR itself (what a
      conforming path looks like, and detecting a non-conforming one) is
      delivered under task 1.1/2.2, not this task.
- [ ] 3.2 NON-GOAL — the authoring-provenance-comment WRITE action (see
      scope note). `canon fmt --check` detects its absence
      (`missing-provenance`, task 2.2); writing it is `canon migrate`'s
      removed job.
- [ ] 3.3 NON-GOAL — `canon migrate`'s `inventory/` rewrite action (see
      scope note); the layout grammar + detection are task 1.1/2.2.
- [ ] 3.4 NON-GOAL — the envelope-backfill WRITE action (see scope note);
      `canon fmt --check` detects a missing envelope (`missing-envelope`,
      task 2.2).
- [ ] 3.5 NON-GOAL — `assets.lock`'s rewrite action (see scope note);
      `canon fmt --check` flags the legacy path as a fourth ad-hoc format
      (task 2.2/2.3).

## 4. Expressiveness upgrades — schema fields + `canon fmt --check` detection

- [ ] 4.1 NON-GOAL — the bare-`by`-string → structured-`actor` WRITE
      action was `canon migrate`'s job (see scope note). The destination
      schema shape (`Actor {agent_id, role: Option<RoleId>, session_id?,
      model?}`, `Actor::new_unattributed`, `by: Option<String>` kept
      additive) is delivered under task 1.1, not this task.
- [x] 4.2 Implement `<file>#<symbol>` ref-string parsing (splitting
      `;`-/`,`-joined strings per segment before parsing each
      independently) — rescoped: this is `canon-fmt`'s own parser, feeding
      `canon fmt --check`'s `free-text-ref`/`joined-ref` detection; it
      never writes a `refs:` array back to any file (that WRITE action was
      `canon migrate`'s job and is NON-GOAL, see scope note).
      Evidence: `crates/canon-fmt/src/refparse.rs` (5 unit tests covering
      single/`;`-joined/`,`-joined/same-file-continuation/free-text
      inputs) + `crates/canon-fmt/src/resolve.rs::parse_ref_field`, used by
      `check.rs::check_ref_field`.
- [ ] 4.3 NON-GOAL — abbreviated-sha RESOLUTION against a live git
      checkout (see scope note) — the `GitShaResolver`/`resolve_via_git`
      that shelled out to `git rev-parse` against a live repo is removed
      entirely (`crates/canon-fmt/src/sha.rs` now only keeps
      `is_full_sha`, the DETECTION `canon fmt --check` uses for the
      `abbreviated-sha` class, task 2.2).
- [x] 4.4 Add optional `change_id`/`task_id` fields to the schema (left
      absent on any record that predates this schema; populated by S4
      ingest going forward).
      Evidence: `crates/canon-model/src/family/ledger.rs`'s
      `LedgerRunRecord`/`LedgerReviewRecord::{change_id, task_id}`.
- [x] 4.5 Add optional `checked` (aspects) content field to review records
      (left absent unless derivable; schema-optional).
      Evidence: `crates/canon-model/src/family/ledger.rs`'s
      `LedgerReviewRecord::checked: Option<Vec<String>>`.
- [x] 4.6 Implement bidirectional back-refs — rescoped: the additive
      `divergence_refs` schema field plus the cross-family DETECTION pass
      (does the ledger record a divergence's `ledger_ref` names carry the
      reciprocal entry?) are shipped; auto-POPULATING `divergence_refs` by
      cross-indexing at migration time was `canon migrate`'s job and is
      NON-GOAL (see scope note).
      Evidence: `crates/canon-model/src/family/ledger.rs`'s
      `LedgerReviewRecord::divergence_refs: Vec<String>` (schema, task
      1.1); `crates/canon-fmt/src/check.rs::{check_ledger,
      check_divergences, BackrefExpectation}` (detection, task 2.2),
      reporting `FmtFailureClass::OneWayBackref`.

## 5. Quarantine report — NON-GOAL

- [ ] 5.1 NON-GOAL — the quarantine report writer (see scope note). There
      is no quarantine report: `canon-fmt/src/quarantine.rs` and
      `canon-cli/src/migrate.rs` are deleted; `canon fmt --check`'s own
      `FmtReport`/`Violation` (task 2.2) is the only report this crate
      produces, and it never writes to disk.
- [ ] 5.2 NON-GOAL — `canon migrate --dry-run` (see scope note); the
      `Migrate` clap subcommand and `run_migrate` are removed from
      `crates/canon-cli/src/main.rs`.
- [ ] 5.3 NON-GOAL — this task's acceptance bar was phrased in terms of a
      `canon migrate`-quarantined record (see scope note); removed along
      with the rest of this group's requirement text in
      `specs/artifact-family-schema/spec.md`.

## 6. parity.py sync patch — NON-GOAL

- [ ] 6.1 NON-GOAL (see scope note) — no `parity.py` patch ships with this
      change.
- [ ] 6.2 NON-GOAL (see scope note) — no `parity.py` patch ships with this
      change.
- [ ] 6.3 NON-GOAL (see scope note) — no `parity.py` patch ships with this
      change.

## 7. In-place migration run + acceptance — NON-GOAL

- [ ] 7.1 NON-GOAL (see scope note) — no `canon migrate` run against any
      live consumer-repo checkout; `canon-fmt` never reads
      the donor consumer repo (or any other live path) on its own.
- [ ] 7.2 NON-GOAL (see scope note).
- [ ] 7.3 NON-GOAL (see scope note) — `parity.py` is untouched by this
      change; nothing here to re-verify against it.

## 8. Fixtures and selftest

- [x] 8.1 Build a LOCAL fixture corpus (small, representative samples of
      each artifact kind, every audited gap category represented at least
      once) reproducing the donor consumer repo's real, audited drift shapes —
      rescoped from "pre-migration and post-migration" to fixtures-only,
      since there is no migration output to fixture (see scope note).
      Evidence: `crates/canon-fmt/fixtures/consumer-corpus/pre/spec/**`
      (ledger, divergences, features, inventory, policy.yaml samples).
- [ ] 8.2 NON-GOAL — the EXPECTED quarantine report fixture and its
      `canon migrate --dry-run` diff test (see scope note); the
      `EXPECTED-quarantine-report.json` fixture file is deleted.
- [x] 8.3 Wire the S11 fixture corpus into `canon selftest` (design doc §8,
      "Testing strategy").
      — ✅ `canon selftest` now exists (Wave-3 `canon_cli::selftest`
      aggregator) and registers `crates/canon-fmt/src/lib.rs::selftest()`
      as the `format-authority` suite (10 checks): it runs `canon fmt
      --check` over the checked-in `fixtures/consumer-corpus/**` corpus
      and asserts every audited drift-gap category is surfaced (the same
      `fixtures_check.rs` logic, now the single source). Green under
      `canon selftest`. (Unrelated to the migrate/the donor consumer repo/parity.py
      NON-GOAL trims above — those stay out of scope.)

## 9. Companion skill

- [x] 9.1 Author the `canon fmt --check` companion skill under
      `canon/skills/` (decision 9): documents `canon fmt --check`, reading
      its violation report, and explicitly what it does NOT do (no
      `canon migrate`, no corpus rewrite, no `parity.py` patch — see scope
      note) — materialized for Claude Code + Codex only via the
      content-hash + version install lock.
      Evidence: `canon/skills/format-authority/SKILL.md` (rewritten,
      content-hash bumped to `.install-lock.json` v2), materialized
      verbatim to `.claude/skills/format-authority/SKILL.md` and flattened
      to `.codex/skills/format-authority.md` via `canon skills install`.
